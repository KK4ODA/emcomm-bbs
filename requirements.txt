# Emcomm BBS - Python dependencies
# Install with:  python -m pip install -r requirements.txt
# Python 3.8 or newer is required. tkinter ships with Python on Windows and
# macOS; on Linux install python3-tk from your package manager.

requests>=2.31.0          # HTTP for every data source
watchdog>=2.1.0           # Welfare Board folder monitoring

# Optional: AI-written news digest. Needs an Anthropic API key in Settings;
# without the package (or the key) plain headline lists are produced.
anthropic>=1.0.0
